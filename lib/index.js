/**
 * Module dependencies.
 */
const Provider = require('./provider');

/**
 * Expose `Provider` directly from package.
 */
exports = module.exports = Provider;

/**
 * Export constructors.
 */
exports.Provider = Provider;
